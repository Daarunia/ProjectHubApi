{
    "version": 2,
    "builds": [
      {
        "src": "server.php",
        "use": "@vercel/php",
        "config": {
          "maxLambdaSize": "15mb"
        }
      }
    ],
    "routes": [
      {
        "src": "/(.*)",
        "dest": "/server.php"
      }
    ]
  }

